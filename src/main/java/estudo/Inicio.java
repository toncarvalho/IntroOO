package estudo;

/**
 * Primeira classe do programa, deve conter o Método main.
 */
public class Inicio {

    /**
     * Método que inicia a aplicação em java.
     * <p/>
     * Observe a assinatura do método, leva a palava reservada static, que possibilita que o método seja executado mesmo sem
     * uma instancia da classe, apenas invocando. O método main tem como argumento um array com de Strings, esta é a
     * assinatura padrão de um método main, este método é o responsável por iniciar a plicação.
     *
     * @param args
     */
    public static void main(String[] args) {


    }
}
